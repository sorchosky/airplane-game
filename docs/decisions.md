# Decisions log

One line per decision. Newest at the bottom. Format: `YYYY-MM-DD · area · decision · why`.
Sessions must not re-litigate these. Propose changes in PR handoff notes instead.

- 2026-09-24 · stack · Vite + React + TS + React Three Fiber + drei + Zustand · owner is fluent in React; R3F keeps 3D and UI in one tree
- 2026-09-24 · stack · WebGL2 renderer, not WebGPU · safest support on iOS Safari; revisit after M4
- 2026-09-24 · pose · MediaPipe Pose Landmarker (lite) via `@mediapipe/tasks-vision`, on-device · free, GPU accelerated, 33 landmarks, no video leaves the device
- 2026-09-24 · pose · One Euro filter for landmark smoothing · low lag matters because TV mirroring already adds ~100–200 ms
- 2026-09-24 · controls · Roll = angle of the wrist-to-wrist line; pitch = both arms raised (climb) or lowered (dive) relative to shoulders · reliable from a single 2D camera; torso lean needs noisy depth
- 2026-09-24 · controls · "Outstretched arms" gate: elbows nearly straight, wrist span > ~1.5× shoulder width, high landmark visibility; 300 ms to engage, 500 ms grace to disengage · avoids flicker
- 2026-09-24 · controls · Arms down or player out of frame → autopilot eases to level flight and shows a prompt · forgiving, keeps flow
- 2026-09-24 · controls · Arms down for 5 s → pause; arms out → resume · player is ~2 m from the phone, so no touch after Start
- 2026-09-24 · platform · Primary setup: phone propped in landscape, front camera, screen mirrored to a TV · owner's play setup; drives 10-foot UI and wake-lock requirements
- 2026-09-24 · flight · Arcade model: constant airspeed, bank causes turn, auto-level, soft terrain floor, no crash death in v1 · adventurous free flight, not a sim
- 2026-09-24 · art · Plane is procedural low-poly in code (Cessna-style high wing), swappable for glTF later · agent-buildable, no asset pipeline
- 2026-09-24 · hosting · Vercel with a preview deploy per PR · HTTPS for camera; test each PR on the phone
- 2026-09-24 · workflow · One ticket = one session = one PR, started manually with `docs/kickoff-prompt.md` · owner controls usage on a Pro plan
- 2026-09-24 · future · Split architecture (phone streams pose over WebRTC to a TV/laptop browser that renders) is out of scope for v1 · better quality and no mirroring lag, but much larger build
- 2026-09-25 · stack · `@react-three/fiber` v9 and `@react-three/drei` v10, not v8/v9 · React 18 peer deps of R3F v8 conflict with the React 19 required by #6; v9 targets React 19
- 2026-09-25 · art · Palette is a warm, low-contrast sunset (dusty gold/terracotta/muted teal) in `src/styles/tokens.ts`/`.css`, not a bright saturated midday look; `accent` is a muted teal-cyan, not neon, reserved for interactive UI rather than the plane livery (cream body, terracotta stripe) · owner review on the initial brighter/cooler palette called it too cartoony; kept one deliberately distinct (but muted) accent color for gesture feedback so it stays meaningful without clashing
- 2026-09-25 · art · Type: `Josefin Sans` (art-deco display, logo/section headers only) + `Work Sans` (Helvetica-like body/HUD everywhere else), both Google Fonts (SIL OFL 1.1), loaded via the Fonts API CSS in `tokens.css`; main title is uppercase with wide tracking (`type.trackingDisplay`), and the whole type ramp leans smaller than a typical hero-game-title scale · owner asked for BotW's actual split (Helvetica-like UI, special font only for logo/headers) and a smaller, mid-century-poster-style ramp instead of a big fantasy display scale; self-hosting can follow later if offline load time becomes an issue
- 2026-09-25 · art · UI buttons are a `surface-hud` panel with a 2px `accent` border, not a solid `accent` fill · matches an outlined-button look and avoids needing a separate "text on accent" token
- 2026-09-25 · art · `text-primary` never sits directly on the raw sky gradient; `TitleScreen`'s title/tagline and `?swatches`'s content both moved onto a `surface-hud` plate · a WCAG audit found `text-primary` on `sky-horizon` at only 1.7:1, far below AA, even though the same text on `surface-hud` passes comfortably; the plate also reads as a mid-century poster device rather than a bolted-on fix
- 2026-09-25 · art · Reverted the above for `TitleScreen` only: no panel behind the title/tagline/Start button, Start is a plain `text-primary`-outlined button (no fill, no `accent` border) · explicit owner request for this round of visual testing over the AA fix; `?swatches` keeps its `surface-hud` sheet. Known gap: title-screen text is not reliably AA-compliant everywhere on the gradient — flagged in `docs/art-direction.md`, revisit before shipping
- 2026-09-25 · art · Start button's label matches the title's type treatment (`fontDisplay`, uppercase, `trackingDisplay`) at `tv-title` size instead of `fontBody`; all drop shadows removed from `TitleScreen` except a minimal zero-offset glow kept on the tagline (`p`) only · owner asked to remove all shadows or soften for accessibility; the title clears the 3:1 large-text minimum on its own at typical gradient positions, the tagline (held to 4.5:1 as normal-weight text) doesn't, so it's the one element that keeps a legibility assist — a soft ambient glow rather than a directional drop shadow
- 2026-09-25 · pose · `src/pose/types.ts` defines its own `PoseLandmark` shape (structurally identical to MediaPipe's `NormalizedLandmark`) instead of importing `@mediapipe/tasks-vision` · lets the gesture interpreter (#16) be built and tested before the pose service (#14) adds the real dependency
- 2026-09-25 · controls · Pitch mapped from mean-wrist-height-over-shoulder-width with deadzone 0.05 / full scale 0.6 of that ratio; roll response uses a mild `^1.4` curve past its linear deadzone/scale · no hardware to tune against yet; initial estimates in `DEFAULT_GESTURE_PARAMS`, expect revision once #19/#27 add real recordings
- 2026-09-25 · stack · `.nvmrc`/`package.json` `engines.node` bumped `22.11.0` → `22.12.0` · CI was silently red on every push: `vite@8.3.1`/`rolldown@1.2.11`/`vitest@5.0.2` all require Node ≥22.12.0 (visible as `npm warn EBADENGINE`, non-fatal to `npm ci`), and on the too-old pinned 22.11.0 rolldown's native-binding loader falls through to an uninstalled WASI fallback and crashes at `vitest` startup — reproduced identically in CI, not reproducible locally on a newer Node
- 2026-09-25 · world · Terrain is 512 m chunks in 4 LOD rings (8/16/32/64 m vertex spacing at 0–1/1–2.5/2.5–5/5–10 km), nothing past 10 km, heights built in a Web Worker, rebuilt only on chunk-boundary crossings, all tunables in `src/world/terrainConfig.ts` · long view distance for a fast flight game at 60 fps on a MacBook Air class laptop; if perf is tight cut view and foliage distance before near-terrain detail
- 2026-09-25 · art · Terrain is colored in the shader from height and slope (no ground textures), replacing #24's original per-chunk vertex colors; foliage only within 1.5 km, thinning to the edge; haze must fully hide the 10 km terrain edge · owner's terrain spec; keeps chunk buffers small and color bands tunable without rebuilding chunks
- 2026-09-25 · workflow · The terrain brief runs as sequential sessions #12 (shape) → #24 (color, water, rivers) → #22 (sky, haze) → #25 (foliage), with owner review between each · keeps one ticket per session while giving the layer-by-layer review the owner asked for
- 2026-09-25 · world · LOD cracks are hidden with skirts (a wall hanging `skirtDepthFactor` × vertex spacing below every tile edge), not edge stitching · works for any pair of neighbouring LODs and keeps one shared index buffer per grid size
- 2026-09-25 · world · Far rings are merged by a quadtree: 2.5–5 km tiles are 2×2 chunks, 5–10 km tiles are 4×4 chunks (both 32×32 quads) · ~270 tiles to 10 km instead of ~1,200 chunks, ~90 draw calls after frustum culling
- 2026-09-25 · world · A new tile layout is built hidden and swapped in all at once when every tile is ready · no holes while loading and no overlap between old and new LODs
- 2026-09-25 · flight · Soft floor: pitch-up bias ramping from 0 to 50° over the lowest 20 m, plus a hard 2 m minimum above ground that wins over the 600 m ceiling; terrain heights tuned to stay under 600 m · 50° is twice max pitch, so a full dive still ends in a pull-up
- 2026-09-25 · pose · MediaPipe WASM is copied from `node_modules` into `public/mediapipe/wasm/` by a `postinstall` script (gitignored); the 5.8 MB `pose_landmarker_lite.task` model is committed to `public/mediapipe/` · no third-party CDN at runtime; the WASM stays in lockstep with the installed package version, and the model isn't on npm, so committing it avoids a network fetch at install time in CI and on Vercel
- 2026-09-25 · pose · The pose service mirrors landmarks into selfie space (normalized `x → 1 − x`, world `x → −x`) and keeps MediaPipe's player-relative left/right indices · the gesture interpreter (#16) expects mirrored input so the player's left arm is on screen-left
- 2026-09-25 · pose · `poseSource` interprets each detection exactly once (keyed on `poseStore.detectedAtMs`), and treats the player as out of frame if no detection lands for 250 ms · feeding the same 20 Hz sample to the One Euro filters at the 60 Hz render rate skews their speed estimate; the stale timeout lets the gesture gate disengage if detection stops (tab hidden, camera stalled)
- 2026-09-25 · controls · Control-state timing lives in a pure machine (`src/app/controlStateMachine.ts`: active → inactive → paused → arming → countdown); every resume, including Esc in keyboard mode, goes through the 3-2-1 countdown, and arms dropping during the countdown cancels it back to paused · one resume path, and the player is never dropped into flight unready
- 2026-09-25 · controls · Keyboard mode (`?input=keyboard` or no flag) never auto-pauses on inactivity; Esc is its only pause toggle · keyboard `active` defaults off, so gesture-style auto-pause would pause every dev session after 5 s
- 2026-09-25 · input · Only the source selected by `?input=` writes the input store (`useKeyboardSource`/`usePoseSource` take an `enabled` flag) · both previously ran at once, and the keyboard source overwrote pose input with `active: false` every frame
- 2026-09-25 · ui · The autopilot prompt is horizontally centered but sits in the lower third (`top: 70%`), not dead center · dead center covers the plane, which the chase camera keeps at screen center
- 2026-09-25 · pose · Calibration captures neutral roll, pitch and shoulder width as the mean over a 2 s steady arms-out hold (reset if roll std > 5°, pitch std > 0.12 or shoulder-width CV > 0.1); the distance check wants shoulders and hips visible with shoulder distance 0.07–0.2 of frame width; a saved calibration is reused after a 1 s hold when the held pose is within 6° roll / 0.15 pitch of it, otherwise the full hold recalibrates · estimates for a phone front camera at ~2 m where a T-pose must fit in frame; all live in `DEFAULT_CALIBRATION_PARAMS`, expect retuning with real recordings (#19)
- 2026-09-25 · art · Outlines are an inverted hull drawn with one shared back-face `ShaderMaterial` per color/weight (not drei `<Outlines>`, which makes a material per mesh), offset `0.06` m along smoothed normals and capped at 3 CSS px on screen; toon band levels are `0.5 / 0.78 / 1` of the light; terrain is toon-shaded but not outlined · exactly one extra draw call per outlined mesh and no extra shader programs; world-unit thickness thins with distance so far geometry never smudges, the pixel cap keeps close-ups from going heavy; outlining ~80 terrain tiles would double terrain draw calls for lines mostly hidden under the surface
- 2026-09-25 · audio · `src/audio/audioEngine.ts` runs two output buses off one `AudioContext`: `master` (engine drone + wind, silenced by mute or the pause duck) and `cueBus` (active/inactive and countdown blips, silenced only by mute); all continuous params use `AudioParam.setTargetAtTime`, never instant sets · the resume countdown has to be audible while the sim is ducked for pause, so cues can't share master's gain node; `setTargetAtTime` composes cleanly across repeated per-frame calls without the discontinuities `cancelScheduledValues` + a fresh ramp would risk
- 2026-09-25 · audio · This round ships only the M-key mute toggle (persisted to `localStorage` via `src/audio/audioStore.ts`), no pause-menu item · #28 (pause menu) hadn't landed yet when #29 ran; wiring a mute item into it is a follow-up
- 2026-09-25 · world · Haze replaces Three's fog chunks globally (`installAtmosphereFog` in `src/world/atmosphereShader.ts`): a warm near layer (`fog` token, exponential, capped at `hazeWarmMax`) plus a far layer that blends to the *sky colour in that exact view direction*, 100% by `hazeFadeEnd` = 9 km (nearest terrain edge is ~9.64 km); the sky dome calls the same GLSL function · a hill that is fully hazed is mathematically the sky behind it, so there is no horizon seam and the 10 km edge can't show from any altitude or heading; patching the chunks means every built-in material, including #21's toon materials if they keep `fog: true`, gets the haze for free
- 2026-09-25 · world · Horizon facing away from the sun shifts `hazeCoolShift` (45%) from `sky-horizon` toward `sky-zenith`; this is how "haze shifts toward blue with distance" is met inside the sunset palette · no new blue token needed and it matches how real sunset skies cool opposite the sun
- 2026-09-25 · world · No shadows and no far mountain silhouette ring · shadows cost a second depth pass over ~90 terrain draws for little read at golden hour; the real terrain already carries mountains out to 10 km, and a painted ring would sit behind a haze whose whole job is to make the far distance pure sky colour
- 2026-09-25 · controls · The pause menu (#28) owns the paused arms-out hold: tilt past |roll| 0.5 moves the highlight one item (re-armed below 0.2, no wrap), a level arms-out hold of 1.5 s picks; the control machine's `arming` phase and `resumeHoldMs` are gone, and Resume goes through the existing 3-2-1 countdown · one hold gesture on the pause screen, so arms out can't both resume and pick a menu item; thresholds are estimates in `DEFAULT_PAUSE_MENU_PARAMS`, retune with #19 recordings
- 2026-09-25 · ui · Player-facing strings live in `src/ui/copy.ts`; the How to play plate on the title screen sits on `surface-hud` (the title/tagline/Start stay plate-free per the earlier owner request); portrait shows an opaque `surface-scrim` blocker and pauses flight · centralizes the voice; multi-line body text needs the plate to clear AA
- 2026-09-25 · art · The plane's control surfaces live inside the merged `plane-stripe` mesh and are bent about their hinges on the CPU (`createArticulation`, ~1k vertices per frame), not split into separate meshes; they're stripe-colored; the prop swaps blades for a single-pass translucent disc at ≥ 92% of cruise speed, and blades spin at 0.15 rev/m (≤ 9 rev/s) · separate outlined meshes for 5 surfaces would add 10 draw calls and blow the ≤ 12 budget (plane is 8 at cruise, 9 with blades); terracotta surfaces make deflection read from the chase camera on a TV; a slow visual prop rate avoids the wagon-wheel strobe at 60 fps
- 2026-09-25 · world · Lakes and rivers are carved into `heightAt` (lakes: a smoothstep scoop under the broad, low-octave land shape below `lakeBasinHeight`; rivers: channels along zero crossings of a low-frequency noise, fading out above `riverMaxHeight`), with one translucent water plane at `waterLevel` = 32 m; shallow/deep blending comes from the terrain shader tinting the lake bed by depth, seen through the surface, not a depth texture · no second heightfield or render pass, and the haze is linear so a hazed bed under a hazed surface stays seamless; lake scoop depth is capped below `lakeBasinBand / 1.5` so shores never grow a rim
- 2026-09-25 · art · Added a `sand` token (`#cdb48a`) for the waterline band; terrain grass varies only between `grass-light` and `grass-shadow` · #24 needs a sand band and "tokens only" forbids a raw hex in the shader
- 2026-09-25 · flight · The soft floor uses `surfaceHeightAt` = max(ground, `waterLevel`), so the plane skims lakes instead of diving to the lake bed · lakes made the ground under the plane lower than the visible surface
- 2026-09-26 · ui · The game is renamed Driftwing, and the title screen follows the owner's Figma storyboard: a frosted backdrop-blur band sweeps left to right while the wordmark is revealed under it, then Start fades up from a 2 px blur. The tagline and the How to play plate are removed, which supersedes the 2026-09-25 How to play entry · owner's design; the intro plays once per page load (not on Quit or the `permission` remount) and is skipped under `prefers-reduced-motion`
- 2026-09-26 · ui · The title background is the Figma "Golden hour cirrus" WebGPU shader ported to GLSL ES 3.00 on its own raw WebGL2 canvas (`src/ui/TitleSky.tsx`), drawn once per resize, with its dawn/day/dusk blend baked into `title-sky-*` / `title-cloud-*` tokens · the repo is WebGL2-only; one static fragment shader doesn't need R3F's scene and frame loop
- 2026-09-26 · ui · The title band and the wordmark reveal are two linear Web Animations over the same duration and span (reveal edge trails the band's leading edge by 3 vw) · they can't drift apart, the band is always off screen at both ends so linear reads as a steady pass, and the inset keeps the clip's hard edge inside the blur
- 2026-09-26 · ui · Start keeps the Figma look (Work Sans 600, 0.3em tracking, `title-start-border`, 2 px radius) but at `tv-body` size, not Figma's 14 px · the 10-foot UI floor wins over the frame's pixel size
- 2026-09-25 · render · Post-processing (#26) renders the scene through `DisplayRenderPass`, which marks the composer's linear buffer as an XR target for the scene draw so three.js tone maps materials (ACES) exactly as on the canvas; the sky and haze convert display sRGB for that buffer via `linearToOutputTexel` · a plain `RenderPass` drops per-material tone mapping and double-encodes the display-sRGB sky/haze, so `?fx=off` and `?fx=high` would differ in more than the effects; a final `ToneMapping` effect would also tone map the sky, which is deliberately written after tone mapping
- 2026-09-25 · render · Quality tiers: `low` = no composer, `medium` = half-res mipmap bloom only, `high` = full-res bloom + warm lift + saturation + vignette, composer MSAA 4×; default `medium` on a coarse primary pointer, `high` otherwise; bloom threshold 0.42 linear luminance, measured so only the sun and its sky glow bloom · all tunables in `src/render/postFx.ts`; #27 owns switching tiers from frame time
- 2026-09-25 · input · With no `?input=` flag (or an unknown value) the source is `pose`, not `keyboard`; keyboard is opt-in with `?input=keyboard`, and the title screen's skip-calibration check reads the same `getInputSourceFromUrl` · the player URL sent people through camera calibration and then steered with the keyboard source, so motion controls never reached the plane and the in-flight camera preview never showed; supersedes the "or no flag" part of the keyboard auto-pause entry above
- 2026-09-26 · debug · The latency probe (`src/debug/latencyProbe.ts`) arms a sample when a `ControlInput` roll jumps by 0.1 and completes it on the first frame whose bank has moved 0.5° from its value at input time, stamped after the sim step (before draw and scan-out); camera stamps use `requestVideoFrameCallback` `captureTime` where the browser reports it and the callback time otherwise (the HUD says "cam est") · one fixed definition so E4's before/after numbers compare like with like; 0.5° is the smallest bank change that reads on a TV, and a photon-accurate stamp isn't available from JS
- 2026-09-26 · input · `?input=replay[&replay=<name>][&loop]` plays `tests/fixtures/replays/<name>.json` (`{ frames: [{ tMs, label?, landmarks, worldLandmarks }] }`, the same shape the `?debug` Record button saves) into `poseStore` from the frame loop at a `detections` priority just ahead of the input sources, starting when the game leaves the title; the camera and MediaPipe never start; without `&loop` the last frame keeps arriving; `window.__driftwing.snapshot()` exists only under `?debug` or `?input=replay` · timer-driven playback was starved behind long software-GL frames and read as a stale detector, dropping the gate mid-tilt; one fixture shape for generated and recorded clips
- 2026-09-26 · ci · CI runs the whole `npm run e2e` suite in a second job (Chromium installed on the runner with `playwright install --with-deps`, results uploaded as an artifact); the title "How to play" spec is deleted because the plate was removed by the Driftwing title decision above, and the countdown specs accept any tick (3, 2 or 1) · e2e had never run in CI, so three specs had rotted unseen; a software-GL runner can miss the "3" tick between polls
- 2026-09-26 · robustness · A lost camera (track `ended`, or `mute` not undone within 3 s) is retried automatically on a 1/2/4/8 s backoff instead of behind a Retry button, and the HUD says "Lost the camera. Reconnecting…" while the control machine drops to autopilot and pauses after 5 s as usual; a hidden tab force-pauses; a lost WebGL context remounts the canvas (flight state lives in stores and carries on); the model-failure state is the one screen after Start that takes a tap · hands-free after Start rules out an in-flight button; the model loads seconds after Start while the player is still at the phone
- 2026-09-26 · workflow · The AAA polish pass (epic M5) runs several PRs per session on `claude/aaa-<slice>` branches cut from current `main`, each one coherent slice with its own screenshots and handoff notes · owner's explicit allowance for this pass; supersedes the one-ticket-per-session rule for M5 only, the other session rules still apply
- 2026-09-26 · debug · `?shot=<name>` parks the plane at a fixed world bookmark (`src/debug/shots.ts`) with the sim, clouds and water frozen and the HUD hidden, and `tests/e2e/shots.spec.ts` (behind `SHOTS=1`) captures every bookmark at 1920×1080 with draw and triangle counts · every art PR compares before and after at the same cameras; the frozen sim keeps a capture repeatable apart from terrain streaming, which the spec waits for
- 2026-09-26 · art · The owner picked palette B, Hyrule morning, as the default (albedo tokens and the `morning` lighting group in `docs/art-bible.md` §3), with golden hour kept as a preset; A1 (#64) builds it and supersedes the 2026-09-25 palette entry; a 5-minute day cycle (day, afternoon, dusk, night, morning) is a follow-up (#92), so A1 puts every lighting value behind a uniform · owner's choice on #88 from the rendered contact sheets
- 2026-09-26 · flight · Speed is energy (#68): `dv/dt = -energyGain·sin(pitch) + (cruise − v)/speedDecayTime`, clamped to `minSpeed..maxSpeed`, with `energyGain` 18 m/s² (about twice real gravity along the path), `speedDecayTime` 2.5 s (80 % of a dive's extra speed gone in ~4 s of level flight) and `maxSpeed` 60 → 68; the turn rate follows the bank lagged by `yawLagTime` 0.15 s; the soft floor reports its depth as `state.floorContact` and `onFloorContact` start/end events (enter 0.05, exit 0.02); all in `FLIGHT_FEEL` · a sustained full dive settles near 64 m/s and a sustained climb bottoms at `minSpeed` 32 (was about 51 and 39), so dives pay out in the next climb; the arms-back boost is not shipped here because it needs the replay fixtures (#62) to prove no false triggers and a `ControlInput` contract change
- 2026-09-26 · art · Palette B replaces the 2026-09-25 warm, low-contrast sunset palette as the default look: `color` holds only albedo and UI tokens, and sky, haze, sun, ambient and cloud colors plus sun direction and intensity move into `lightingPresets` (`morning` default, `goldenHour` via `?tod=golden`), delivered to every material through shared `Float32Array` uniforms in `src/world/atmosphereUniforms.ts`; toon bands light at 0.62 / 0.86 / 1 with hemisphere sky fill; bloom threshold 0.9 with the sun disc drawn at HDR ×3 · owner's pick on #88 (#64). Supersedes the sunset palette line; the "cream body, terracotta stripe" livery survives as warm white + safety orange
- 2026-09-26 · art · The plane's readability test is outline ≥3:1 against sky zenith, horizon and grass, stripe ≥3:1 against the horizon, and body ≥3:1 against `grass-shadow` and `water-deep`, instead of #64's "body vs `grass-light` and `sky-horizon` ≥3:1" · with palette B's warm-white body those two pairs are 2.1:1 and 1.1:1 and can't pass without abandoning the approved palette; the plane reads through its cool dark outline (11.4:1 on the horizon) and orange stripe, as in BotW. `tokens.test.ts` records the two low pairs so a change to them is deliberate
- 2026-09-26 · art · Terrain surface detail (#69) is shader noise, no textures: 400 m macro hue ±6° and value ±6 %, 35 m brush ±4 % streaked down the fall line by three taps along the downhill direction (offset taps, not rotated coordinates, so the pattern doesn't swim as the normal turns), rock strata 10–16 m apart with an `fwidth` fade, and a sun-facing grass tint whose cool side is `grass-shadow` leaning toward the preset's `ambientSky` hue at `grass-shadow`'s luminance · #69 asked for 4–7 m strata, which read as pinstripes and moiré from flight height; the raw sky mix lightened shaded slopes because the sky is several times brighter than grass in linear light
